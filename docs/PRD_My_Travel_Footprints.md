# 요구사항 정의서 (PRD) - 나의 여행 발자취 (My Travel Footprints)

## 1. 프로젝트 개요
* **프로젝트명:** 나의 여행 발자취 (My Travel Footprints)
* **목적:** 개인의 여행 기록(날짜, 제목, 위치 등)을 타임라인 카드로 아카이빙하고, 지도 인터랙션을 통해 시각적으로 한눈에 확인할 수 있는 개발자 포트폴리오용 웹 애플리케이션
* **타겟 사용자:** 본인의 여행 기록을 시각적으로 관리하고 공유하고자 하는 개인 (또는 포트폴리오 검토자)

## 2. 기술 스택 및 개발 환경
* **Frontend:** React.js / Vue.js (Vite 빌드 도구 및 개발 서버 활용, `localhost:5173`)
* **Styling:** Tailwind CSS 또는 모던 CSS 프레임워크 (Dark / Light 테마 시스템 적용)
* **Map Library:** Leaflet (오픈스트리트맵 기반 지도 렌더링 및 커스텀 마커 표시)

## 3. UI/UX 및 공통 요구사항
* **테마 시스템:** 기본적으로 시각적 피로가 적은 어두운 톤의 다크 테마(Dark Mode)를 제공하며, 우측 상단의 토글 버튼을 통해 라이트 모드로 전환이 가능해야 함.
* **반응형 디자인:** 데스크톱 브라우저 환경에 최적화하되, 싱글 컬럼 레이아웃 구조를 활용하여 모바일 디바이스에서도 지도가 깨지지 않고 하단에 자연스럽게 배치되도록 함.
* **컴포넌트 일관성:** UI 컴포넌트 간의 일관된 보라색/네이비 계열 포인트 컬러 시스템을 유지하여 시각적 몰입감을 높임.

---

## 4. 기능적 요구사항 (Functional Requirements)

### 4.1. 여행 기록 목록 (타임라인 카드 컴포넌트)
| 기능 ID | 기능명 | 상세 요구사항 | 우선순위 |
| :--- | :--- | :--- | :--- |
| **FR-01** | 여행 기록 조회 | - 사용자가 등록한 여행 발자취 리스트를 세로형 카드 형태로 나열함.<br>- 최신순 또는 날짜순 배열 정렬 기능 제공. | High |
| **FR-02** | 데이터 메타 정보 표시 | - 각 카드는 **[방문 날짜(YYYY-MM-DD)], [여행 제목/내용]** 정보를 명확하게 노출함.<br>*(예: 2026-05-21 부산 여행)* | High |
| **FR-03** | 여행 기록 수정 (Update) | - 카드 좌측 하단의 '연필 모양(수정)' 버튼 클릭 시, 해당 기록의 제목 및 날짜를 수정할 수 있는 입력 폼(또는 모달) 진입. | High |
| **FR-04** | 여행 기록 삭제 (Delete) | - 카드 좌측 하단의 '휴지통 모양(삭제)' 버튼 클릭 시, 해당 기록이 리스트와 지도 마커에서 즉시 삭제됨 (삭제 전 확인 컨펌창 노출). | High |

### 4.2. 지도 인터랙션 (Map Integration 컴포넌트)
| 기능 ID | 기능명 | 상세 요구사항 | 우선순위 |
| :--- | :--- | :--- | :--- |
| **FR-05** | 지도 렌더링 및 스타일 | - Leaflet 라이브러리를 활용하여 하단 영역에 지도를 표현함.<br>- 웹사이트 전체 톤앤매너에 맞게 어두운 스타일(Dark Tile) 지도 레이어 적용. | High |
| **FR-06** | 위치 마커(Marker) 표시 | - 타임라인 리스트에 등록된 여행지의 위경도 데이터를 기반으로 지도 위에 커스텀 보라색 핀(Marker) 아이콘을 매핑함. | High |
| **FR-07** | 지도 컨트롤 (줌 인/아웃) | - 지도 좌측 상단에 `+`, `-` 버튼을 배치하여 사용자가 자유롭게 확대/축소할 수 있도록 함. | Medium |
| **FR-08** | Reset (초기화) 기능 | - 사용자가 지도를 이동하거나 확대/축소했을 때, `Reset` 버튼을 누르면 초기 설정된 중심 좌표와 줌 레벨로 카메라가 복귀함. | Medium |
| **FR-09** | Fit (화면 맞춤) 기능 | - `Fit` 버튼 클릭 시, 현재 등록된 모든 마커(여행지)들이 한 화면에 가득 차게 보이도록 지도의 영역(Bounds)을 자동으로 조정함. | Medium |

---

## 5. 비기능적 요구사항 (Non-Functional Requirements)
* **성능 및 반응성:** 싱글 페이지 안에서 리스트 추가/수정/삭제 시 지도 마커가 지연 없이(Real-time) 즉각 업데이트되어 반영되어야 함.
* **데이터 관리:** 초기 개발 단계에서는 인메모리 상태(State)로 관리하되, 영속성을 위해 로컬 스토리지 연동을 고려함.

---

## 6. 향후 고도화 백로그 (Backlog)
* **백로그-01 (상세 기록 팝업):** 지도 위의 마커를 클릭하면 해당 여행의 제목과 간단한 한 줄 메모, 사진이 말풍선(Popup)으로 뜨는 기능
* **백로그-02 (여행 기록 추가 폼):** 화면 내에 [새 발자취 남기기] 폼을 추가하여 사용자가 직접 날짜, 제목, 위치를 입력하고 동적으로 카드를 추가할 수 있는 기능
* **백로그-03 (데이터 영속화):** 브라우저 `localStorage` 저장 기능 추가 또는 Firebase / Supabase 등의 Lightweight DB 연동을 통한 데이터 보존