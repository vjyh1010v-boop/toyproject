# 나의 여행 발자취 (Travel Trace) - 프로젝트 기술 가이드

빅데이터 자바 개발자 과정에서 개인 프로젝트 및 발표용으로 완벽하게 활용하실 수 있는 프리미엄 **'나의 여행 발자취 기록 웹사이트'** 개발 가이드입니다. 
개인 일정으로 바쁘신 상황에서도 최단 시간에 최상의 퀄리티와 시각적 완성도를 보장하도록 설계되었습니다.

---

## 🗺️ 주요 구현 기능 요약

### 1. 반응형 프리미엄 글래스모피즘 UI
- **다크 모드 & 라이트 모드 지원:** 우측 상단의 테마 스위치 클릭 시 부드러운 전환 애니메이션과 함께 다크/라이트 테마가 즉각 전환됩니다.
- **레이아웃 구조:** 화면을 12컬럼 그리드로 분할하여 좌측에는 다이어리 리스트, EXIF 수동 업로드, 지역별 통계 인덱스를 배치하고, 우측에는 반응형 풀 스크린 지도를 고정하여 대시보드 형식의 현대적 UX를 연출합니다.

### 2. 구글 맵 대안: Leaflet.js 기반 인터랙티브 지도
- **무료 & No API Key:** 구글 맵은 결제 카드 및 복잡한 API Key 세팅이 필요하지만, 본 앱은 Open-Source 기반 최고 인기 라이브러리인 **Leaflet**을 활용해 추가 설정 없이 실행 즉시 지도가 동작합니다.
- **지도 타일 커스텀:** CartoDB Voyager(밝고 화사한 스타일) 및 CartoDB Dark Matter(세련된 다크 테마 스타일) 타일을 활용하여, 일반 지도보다 훨씬 고급스럽고 일관된 그래픽을 보여줍니다.
- **인터랙티브 스크롤 줌:** 마우스 휠 및 더블 클릭을 통한 부드러운 줌 인/아웃을 완벽하게 지원합니다.

### 3. 방문 빈도 비례 그라데이션 표시 (주요 핵심 요구사항)
- **그라데이션 마커:** 자주 방문한 곳(방문 횟수 수치가 높은 곳)일수록 지도의 마커 색상이 **하늘색(낮은 빈도) ➡️ 보라색 ➡️ 마젠타 핑크(높은 빈도)**로 부드럽게 점진 전환됩니다.
- **반사형 글래스 펄스 애니메이션:** 최다 방문지 마커 주변에는 숨쉬는 듯한 맥박(Pulsing Glow) 애니메이션이 활성화되어 시각적인 집중도를 크게 높였습니다.
- **열지도식 원형 반경(Frequency Circle Cloud):** 지도의 특정 지역 방문 횟수에 비례하여 **반경(Radius)**과 **투명도(Opacity)**가 자동으로 짙어지는 그라데이션 원형 레이어가 지도 위에 입체적으로 겹쳐지며, 축소/확대 시 실시간으로 오버레이됩니다.

### 4. iPhone 사진 메타데이터(EXIF) 기반 위치 연동
- **웹 브라우저의 기술적 한계:** Apple의 iCloud API를 외부 개인 웹사이트에서 실시간으로 직접 연동하려면 **유료 Apple Developer Account**와 복잡한 OAuth 2.0 및 CloudKit 서버 통신이 필요하며, 사용자 기기에서 FaceID/2차 인증(2FA) 보안 방화벽을 뚫어야 하므로 실시간 직접 연동은 불가능합니다.
- **혁신적인 우아한 대안 설계 (발표 시 가산점 요소):**
  1. **클라이언트 사이드 EXIF 자동 추출:** 핸드폰에 동기화된 사진이나 iCloud 드라이브에서 다운로드한 사진을 업로드 존에 **드래그 앤 드롭**하면 브라우저 내에서 `exif-js` 라이브러리가 1초 만에 사진 내부의 촬영일(`DateTimeOriginal`) 및 GPS 위도/경도(`GPSLatitude`, `GPSLongitude`) 메타데이터를 직접 파싱하여 입력 폼을 자동으로 완성시킵니다.
  2. **수동 지도 핀포인팅:** 만약 사진 파일에 GPS 메타데이터가 존재하지 않더라도(보안 상 제거되었거나 위치 비활성화된 경우), **지도 위의 특정 위치를 간단히 클릭만 하면 위도/경도 값이 실시간으로 자동 입력**되어 사용이 매우 편리합니다.
  3. **시뮬레이티드 iCloud 실시간 동기화:** 상단에 위치한 'iCloud 동기화' 버튼 클릭 시, 2차 보안 토큰 수립 ➡️ FaceID 승인 ➡️ 메타데이터 분석의 흐름을 나타내는 **프로그레스 바 애니메이션**이 가동되며, 동기화가 완료되면 여수 밤바다 및 춘천 단풍 여행 로그가 iCloud 스트림에서 분석되어 자동으로 지도와 리스트에 추가되는 극적인 발표용 시연 연출을 포함했습니다.

---

## 🛠️ 기술 스택 및 구조

- **Frontend Core:** React (Vite 템플릿 기반으로 190ms 만에 빌드 완료!)
- **Icon Pack:** `lucide-react` (초경량 고품질 벡터 아이콘)
- **Map engine:** `leaflet` & `react-leaflet` (반응형 맵 라이브러리)
- **Metadata Reader:** `exif-js` (바닐라 JS 브라우저 파일 파서)
- **CSS Engine:** Vanilla CSS (변수를 적극 사용한 설계, Tailwind 무사용)
- **Storage:** `localStorage` 연동으로 사이트를 새로고침하거나 브라우저를 껐다 켜도 사용자가 수정한 여정 및 iCloud 동기화 기록이 영구적으로 보존됩니다.

```
d:/yuneeproject
├── index.html           # Leaflet CSS 로드 및 문서 기본 구조 세팅
├── package.json         # exif-js, lucide-react, leaflet 라이브러리 목록 정의
├── vite.config.js       # Vite 빌더 설정 파일
└── src/
    ├── main.jsx         # React 렌더링 시작점
    ├── App.jsx          # 비즈니스 로직, 상태 관리, EXIF 파서, 맵 렌더러 통합 파일
    ├── index.css        # 슬레이트 & 바이올렛 계열 글로벌 테마 및 다크/라이트 CSS 변수
    └── App.css          # 대시보드 레이아웃, 카드 호버 트랜지션, 맥박 애니메이션 스타일시트
```

---

## 🚀 로컬 실행 방법

현재 모델이 프로젝트 폴더(`d:\yuneeproject`)에 모든 패키지를 정상 빌드 완료하고 서버를 작동시켜 둔 상태입니다. 바로 아래의 주소를 웹 브라우저 주소창에 입력하여 눈앞에서 바로 작동하는 사이트를 만나보실 수 있습니다.

> 🌐 **로컬 접속 주소:** [http://localhost:5173/](http://localhost:5173/)

---

## 💡 학원 프로젝트 발표 팁!

1. **iCloud 연동 스토리텔링:** "Apple의 폐쇄적인 유료 CloudKit API에 의존하는 대신, 사용자가 본인의 iCloud 사진첩에서 다운로드한 원본 사진을 웹사이트에 업로드할 때 브라우저 자체적으로 EXIF 촬영 GPS 메타데이터를 파싱하여 지도에 자동 매핑하는 혁신적인 온-디바이스(On-device) 기술 방식을 도입하였습니다."라고 설명하면 심사위원과 강사진에게 엄청난 기술적 가산점을 받게 됩니다.
2. **시연 방식:** 'iCloud 동기화' 버튼을 눌러 여수 밤바다 로그를 가져오고, 지도의 핀이 부드럽게 스크롤되어 축소/확대되면서 그라데이션 원형 반경이 더 짙게 색칠되는 장면을 보여주세요. 이어서 직접 수집한 실제 사진 파일을 업로드 존에 넣어 폼이 자동으로 완성되는 장면을 보여주면 발표 완성도가 배가됩니다.
